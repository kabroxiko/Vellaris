# VITE_HMR_HOST=vellaris.lonecesito.cl
VITE_HMR_HOST=localhost
# Use the Vite dev server port for local development
VITE_HMR_PORT=5173
# Use plain ws locally; set to 'wss' when behind an HTTPS reverse proxy
VITE_HMR_PROTOCOL=ws
VITE_API_BASE=http://localhost:8080/api
# legacy non-Vite var (unused by client) kept for reference
API_BASE=http://localhost:8080
